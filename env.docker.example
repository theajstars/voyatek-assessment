# Backend
PORT=8080
NODE_ENV=production
JWT_SECRET=change-me
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# MySQL (Docker Compose local)
MYSQL_ROOT_PASSWORD=rootpass
MYSQL_DATABASE=voyatek
MYSQL_USER=voyatek
MYSQL_PASSWORD=voyatekpass

# Prisma connection string (Compose will override host to db)
# DATABASE_URL is set in docker-compose.yml for the api service
# DATABASE_URL="mysql://voyatek:voyatekpass@db:3306/voyatek"

# Railway (set in Railway dashboard)
# DATABASE_URL=mysql://USER:PASSWORD@HOST:PORT/DB?ssl-mode=REQUIRED
